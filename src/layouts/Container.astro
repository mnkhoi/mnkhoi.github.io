---
const { class: className, ...rest } = Astro.props;
---

<div
  id="container"
  class:list={[
    "flex flex-col h-screen w-full bg-[var(--background0)]",
    className,
  ]}
  {...rest}
  ]
>
  <slot />
</div>
