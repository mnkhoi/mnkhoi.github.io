/** @type {import('next').NextConfig} */

let assetPrefix = ''
let basePath = ''


const nextConfig = {
  reactStrictMode: true,
  assetPrefix: assetPrefix,
  basePath: basePath,
  output: 'export',
}

module.exports = nextConfig
